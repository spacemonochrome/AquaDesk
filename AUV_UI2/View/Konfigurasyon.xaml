<UserControl x:Class="AquaDesk.View.Konfigurasyon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AquaDesk.View"
             mc:Ignorable="d"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             d:DesignHeight="1688" d:DesignWidth="1298">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Sol Panel -->
        <StackPanel Grid.Column="0" Margin="20" VerticalAlignment="Top" Width="600">
            <!-- Başlık ve ComboBox -->
            <TextBlock Text="Yön Hareketi" Style="{StaticResource Arayuztext}" Margin="0,0,0,5"/>
            <ComboBox Width="180" Height="30" Margin="0,0,0,15"/>

            <!-- Hareket Kaydet / Kaydı Sil -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15">
                <Button Height="40" Width="85" Margin="0,0,10,10" Style="{StaticResource ModernButton}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="Save" Width="20" Margin="0,0,5,0"/>
                        <TextBlock Text="Kaydet" Style="{StaticResource butontext}"/>
                    </StackPanel>
                </Button>
                <Button Height="40" Width="85" Style="{StaticResource ModernButton}" Margin="0,0,10,10">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconBlock Icon="DeleteLeft" Width="20" Margin="0,0,5,0"/>
                        <TextBlock Text="Sil" Style="{StaticResource butontext}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Conf İçeri Aktar -->
            <TextBlock Text="Conf İçeri Aktar" Style="{StaticResource Arayuztext}" Margin="0,0,0,10"/>
            <Button Height="40" Width="180" Margin="0,0,0,10" Style="{StaticResource ModernButton}">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="Save" Width="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Conf Aktar" Style="{StaticResource butontext}"/>
                </StackPanel>
            </Button>

            <!-- Log Gönder -->
            <Button Height="40" Width="180" Margin="0,0,0,20" Style="{StaticResource ModernButton}">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="Upload" Width="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Log Gönder" Style="{StaticResource butontext}"/>
                </StackPanel>
            </Button>

            <!-- Harfler -->
            <TextBlock Text="A B C D E F G H I J" FontSize="16" Style="{StaticResource Arayuztext}" Margin="0,0,0,5"/>
           
            <!-- Log Göster / Log Kaydet -->
            <Button Height="40" Width="180" Margin="0,0,0,10" Style="{StaticResource ModernButton}">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="Eye" Width="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Log Göster" Style="{StaticResource butontext}"/>
                </StackPanel>
            </Button>

            <Button Height="40" Width="180" Style="{StaticResource ModernButton}" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <fa:IconBlock Icon="Save" Width="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Log Kaydet" Style="{StaticResource butontext}"/>
                </StackPanel>
            </Button>
        </StackPanel>


        <!-- Sağ Panel -->
        <Grid Grid.Column="1" Margin="20,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Test Motoru -->
                <RowDefinition Height="Auto"/>
                <!-- Motor Konumları -->
                <RowDefinition Height="Auto"/>
                <!-- Motor Düzeneği -->
                <RowDefinition Height="*"/>
                <!-- ARACIN İLERİ YÖNÜ -->
            </Grid.RowDefinitions>

            <!-- Test Motoru -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right" Margin="0,0,0,15">
                <TextBlock Text="Test Motoru" Style="{StaticResource Arayuztext}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="60" Height="30" Margin="0,0,10,0"/>
                <Button Width="100" Height="40" Style="{StaticResource ModernButton}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <fa:IconBlock Icon="Play" Width="20" Margin="0,0,5,0"/>
                        <TextBlock Text="Test Et" Style="{StaticResource butontext}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Motor Konumları / İptal -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,0,15">
                <Button Width="200" Height="40" Margin="0,0,10,0" Style="{StaticResource ModernButton}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <fa:IconBlock Icon="Save" Width="20" Margin="0,0,5,0"/>
                        <TextBlock Text="Konumları Kaydet" Style="{StaticResource butontext}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Width="40" Height="40" Style="{StaticResource ModernButton}">
                    <fa:IconBlock Icon="Xmark" Width="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>

            <!-- Motor Düzeneği -->
            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,0,0,15">
                <TextBlock Text="Motor Düzeneği" Style="{StaticResource Arayuztext}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="100" Height="30"/>
            </StackPanel>

            <!-- ARACIN İLERİ YÖNÜ -->
            <StackPanel Orientation="Vertical" Grid.Row="3" HorizontalAlignment="Left" Margin="0,20,0,0">
                <TextBlock Text="ARACIN İLERİ YÖNÜ"  Style="{StaticResource Arayuztext}" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <fa:IconBlock Icon="ArrowUp" FontSize="35" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>



        <!-- Alt Panel (PID Alanları) -->
        <StackPanel Orientation="Horizontal" 
            Grid.ColumnSpan="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="0,20,0,0"
            >

            <TextBlock Text="pid P :" Margin="0 10 0 5" Style="{StaticResource icerik}" FontWeight="SemiBold"/>
            <TextBox Height="30" Width="144" FontSize="16" Margin="10 0 10 0" Style="{StaticResource kutucuk}" Foreground="White"/>

            <TextBlock Text="pid I :" Margin="0 10 0 5" Style="{StaticResource icerik}" FontWeight="SemiBold"/>
            <TextBox Height="30" Width="144" FontSize="16" Margin="10 0 10 0" Style="{StaticResource kutucuk}" Foreground="White"/>

            <TextBlock Text="pid D :" Margin="0 10 0 5" Style="{StaticResource icerik}" FontWeight="SemiBold"/>
            <TextBox Height="30" Width="144" FontSize="16" Margin="10 0 10 0" Style="{StaticResource kutucuk}" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
